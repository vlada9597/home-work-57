# 🧩 Home-work-57 — Розробка HTTP сервера з використанням чистого Node.js

## 🎯 Мета завдання
Розробити базовий HTTP сервер, використовуючи **вбудований модуль `http`** у Node.js.  
Сервер повинен обробляти **GET** і **POST** запити, повертати HTML-відповіді та обробляти помилки.

---

## ⚙️ Функціональні вимоги

### 1. Основні маршрути
| Метод | Шлях | Опис | Код відповіді |
|-------|------|------|----------------|
| `GET` | `/` | Домашня сторінка | 200 |
| `GET` | `/about` | Інформація про сервер | 200 |
| `GET` | `/contact` | Контактна форма | 200 |
| `POST` | `/submit` | Обробка форми (ім’я та email) | 200 або 400 |
| `GET` | будь-який інший | Помилка: сторінку не знайдено | 404 |

---

## 📡 Логіка обробки запитів

### 🔹 GET-запити
Повертають звичайні HTML-сторінки з контентом, створеним через `createHTML()`.

### 🔹 POST-запити до `/submit`
1. Сервер приймає дані з форми у форматі `application/x-www-form-urlencoded`.
2. Якщо `name` або `email` порожні — повертає **400 Bad Request**.
3. Якщо дані валідні — повертає **200 OK** з повідомленням “Form submitted successfully”.

---

## 🚫 Обробка помилок

| Тип помилки | Причина | Код статусу | Повідомлення |
|--------------|----------|--------------|---------------|
| Некоректний маршрут | URL не існує | 404 | Page Not Found |
| Порожні поля у формі | `name` або `email` пусті | 400 | Bad Request: Missing fields |
| Перевищення розміру тіла | POST > 1 MB | 413 | Payload Too Large |
| Помилка на сервері | Внутрішня помилка | 500 | Internal Server Error |

---

## 🧠 Ключові особливості реалізації

- ✅ Використовується лише **вбудований модуль `http`** (без Express).
- ✅ Сервер обробляє **як GET, так і POST** запити.
- ✅ Дані форми парсяться вручну.
- ✅ Є перевірка **обмеження на розмір тіла запиту** (≤ 1MB).
- ✅ Повна система обробки помилок (400, 404, 413, 500).
- ✅ HTML формується динамічно функцією `createHTML()`.

---

## 🚀 Як запустити сервер

### 1️⃣ Встанови Node.js
Переконайся, що Node.js встановлено:
node -v

2️⃣ Запуск проєкту
node server.js

Сервер за замовчуванням слухає порт 3000
Доступ: http://localhost:3000

------------

🧪 Тестування роботи сервера

🔹 1. Головна сторінка
curl.exe http://localhost:3000/
✅ Відповідь: 200 OK — “Welcome to Home Page”

🔹 2. Сторінка “About”
curl.exe http://localhost:3000/about
✅ Відповідь: 200 OK — “About Page”

🔹 3. Контактна форма
curl.exe http://localhost:3000/contact
✅ Відповідь: 200 OK — “Contact Page”

🔹 4. Надсилання форми (POST)
curl.exe -X POST -d "name=John&email=john@email.com" http://localhost:3000/submit
✅ Відповідь: 200 OK — “Form submitted successfully!”

🔹 5. Порожні поля (Bad Request)
Invoke-WebRequest -Uri "http://localhost:3000/submit" -Method POST -Body "name=&email=" -ContentType "application/x-www-form-urlencoded"
⚠️ Відповідь: 400 Bad Request — “Missing fields”

🔹 6. Надто великий запит (>1MB)
$bigData = "a=" + ("x" * 2000000)
Invoke-WebRequest -Uri "http://localhost:3000/submit" -Method POST -Body $bigData -ContentType "application/x-www-form-urlencoded"
⚠️ Відповідь: 413 Payload Too Large

🔹 7. Невідомий маршрут
curl.exe -i http://localhost:3000/unknown
⚠️ Відповідь: 404 Not Found

📁 Структура проєкту

home-work-57/
│
├── server.js          # Основний серверний код
├── README.md          # Документація
└── package.json       # Опис залежностей (якщо створювався через npm init)

💬 Приклад консолі після запуску
Server is running on http://localhost:3000
GET / -> 200
GET /about -> 200
POST /submit -> 200
POST /submit (too large) -> 413


🧰 Використані технології
Node.js (v18+)

Модуль http

Вбудовані методи обробки потоків req.on('data') та req.on('end')

Робота з URLSearchParams для парсингу POST-даних

🏁 Висновок
У межах ДЗ було створено повноцінний HTTP сервер на чистому Node.js, який:

обробляє GET та POST запити;

валідує дані користувача;

реагує на помилки відповідними кодами статусу;

дотримується вимог REST та принципів безпечної обробки запитів.

Сервер готовий до використання як навчальний приклад базового бекенду без фреймворків.
